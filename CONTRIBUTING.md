# 贡献指南

1. 从 `docs/skill-template.md` 复制条目模板，放入对应的 `catalog/<分类>/` 目录。
2. 用自己的中文说明写清用途、功能和适用场景；不要复制平台或原作者的长篇文案。
3. 填写可访问的来源链接、采集日期、热度/评价快照和风险备注。未确认的许可证必须写为“未核验；不复制原始文件”。
4. 先运行 `npm run check`，再以 `catalog/<主题>` 分支提交并发起 Pull Request。
5. 一个 PR 建议新增 5–10 项；审核人检查链接、重复项、许可证与安全风险后再合并。

不要提交 `.idea/`、`.vscode/`、密钥、个人数据或未获再分发许可的原始 Skill 内容。
